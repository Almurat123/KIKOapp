# KIKO App - Web3 AI 交易助手平台

## 📖 产品概述

KIKO App 是一个通过 AI 聊天界面统一所有 Web3 操作的平台。用户通过自然语言与 AI 交互，完成市场查询、数据分析、交易执行等操作，无需浏览复杂的 DEX 界面。

**核心定位**：AI 只负责理解用户意图和执行用户明确指令，**不会替用户做投资决策，也不会预测价格**。

### 核心模块
1. **Chat** - AI 聊天主界面
2. **News** - 新闻情报页面
3. **MarketData** - 市场数据面板
4. **SuperDefi** - DeFi 协议整合

### 核心原则
- **仅提供信息**：所有功能仅提供数据查询和分析，不提供投资建议
- **用户自主决策**：所有交易决策由用户做出
- **跨链整合**：支持所有主流 EVM 链，未来扩展 Solana
- **统一入口**：通过 AI 聊天界面完成所有操作

---

## ✨ 核心功能

### 1. 全链支持
- 支持所有 EVM 链（Ethereum、BNB、Base、Arbitrum、Polygon 等）
- 未来计划支持 Solana

### 2. AI 智能交互
用户通过聊天框完成所有操作：
- **查询功能**：代币信息、价格、流动性、交易量、风险评分
- **交易功能**：Swap 交易、授权管理
- **自动化功能**：价格触发、钱包监控、自动交易策略
- **数据分析**：链上事件、大户行为、市场动态

### 3. 自动交易系统
基于用户授权额度（ERC20 Allowance）的自动化交易：
- **事件触发**：价格变化、流动性变化、特定钱包行为
- **时间触发**：定时任务（计划中）
- **钱包跟踪**：监控指定地址的交易行为
- **安全机制**：不托管私钥，用户可随时撤销授权

### 4. 钱包管理
- **托管钱包**：通过 Privy 登录自动生成（可导出助记词）
- **自托管钱包**：连接 MetaMask/Rainbow/Farcaster/Binance/WalletConnect 等，授权代币额度后自动执行
- **密钥合规**：AI 不生成或传输助记词/私钥；导出仅在用户本地钱包端完成；系统不留存密钥

### 5. 市场数据面板
独立页面展示：
- MarketData
- News
- SuperDefi

---

## 🏗️ 系统架构

### 数据流
```
用户聊天 → AI 意图解析 → Intent JSON → Agent API → 执行引擎 → 区块链
```

### 核心组件

#### 1. AI 层（前端 + AI Model）
- **职责**：理解用户自然语言，转换为结构化 Intent JSON
- **不负责**：直接与链交互、做投资决策、预测价格

#### 2. Intent 解析层
将 AI 输出的自然语言转换为标准化 JSON 格式：
```json
{
  "version": "1.0",
  "intent_id": "uuid-...",
  "correlation_id": "chat-...",
  "origin": "chat",
  "action": "auto_buy",
  "token_in": "0xUSDC...",
  "token_out": "0x...",
  "amount": "100",
  "amount_asset": "USDC",
  "chain_id": 8453,
  "slippage_bps": 50,
  "deadline_s": 600,
  "max_gas": "auto",
  "allowance_mode": "one_shot",
  "validation": ["price_available", "liquidity_threshold", "volume_threshold"],
  "trigger": {
    "type": "price_drop_pct",
    "value": 30,
    "window_s": 300,
    "min_duration_s": 120
  }
}
```

#### 3. Agent API 层
提供以下 API 端点：
- `GET /agent/tokens/:address` - 查询代币信息
- `POST /agent/strategies` / `PATCH /agent/strategies/:id` / `DELETE /agent/strategies/:id` - 策略管理
- `POST /agent/tx/plan` - 生成交易计划
- `POST /agent/tx/preview` - 预览交易步骤（含风险与估算）
- `POST /agent/tx/execute` - 执行交易（用户确认后）
- `POST /agent/triggers` / `DELETE /agent/triggers/:id` / `GET /agent/triggers` - 触发器管理
- `GET /agent/queue` - 查看任务队列
- `GET /agent/audit` - 查看操作审计日志
- `GET /agent/status` / `GET /agent/health` - 系统状态
- `POST /agent/wallet/allowance` - 钱包授权管理（不涉及私钥）
- `POST /agent/report` - 生成页面分析报告卡片（AIReportCard）
- `POST /agent/link` - 创建跨页联动动作（AgentLink）

#### 4. 执行引擎
- **标准流水线**：`plan → risk_check → preview → confirm → execute`
- **交易构建**：使用 0x API 生成 Swap 交易（白名单路由）
- **风险检测**：执行前进行风险扫描并生成 `risk_id`
- **授权管理**：处理 Approve + Swap 两步流程
- **审计记录**：保存报价快照、风险结果、用户决策、`tx_hash`
- **页面联动**：在 `preview/execute` 阶段推送 `AIReportCard` 到相关页面（如 MarketData/News/SuperDefi），供用户查看与操作

#### 5. 策略引擎
- 保存用户创建的自动交易策略
- 校验策略合法性
- 限制最大交易额度
- 支持策略的创建、更新、撤销
- **限额与节流**：`max_usd_per_day`、`max_trades_per_day`、`cooldown_s`
- **风险拒绝**：对小池或高税费代币强制拒绝或二次确认

#### 6. 触发器系统
- 注册监听条件（价格、钱包行为等）
- 条件满足时触发策略执行
- 任务队列管理
- **标准触发器类型**：`price_cross`、`price_drop_pct`、`liquidity_change_pct`、`wallet_activity`、`volume_spike`、`social_mention_score`
- **去抖与持续**：所有触发器支持 `window_s` 与 `min_duration_s`

#### 7. 风险防护
每次交易前执行检查：
- 计算交换价格与市场价格的偏差，偏差大于 50% 禁止交易
- 价格数据可用性
- 流动性充足性
- 交易量充足性
- 代币税费检测
- 流动性锁定检测

---

## 📋 AI Agent 行为规范

### ✅ AI 必须做的事
1. **理解用户意图**：将自然语言转换为结构化 Intent
2. **提取参数**：从用户输入中提取代币地址、金额、链等信息
3. **生成确认卡片**：显示 TokenCard 或 StrategyCard 供用户确认
4. **执行风险提示**：在交易前提示可能的风险
5. **结构化输出**：所有输出必须符合卡片格式

### ❌ AI 禁止做的事
1. **不能推荐买卖**：不提供任何投资建议
2. **不能预测价格**：不分析涨跌原因或趋势
3. **不能自动决策**：不根据概率帮用户调整仓位
4. **不能创建模糊策略**：策略必须包含完整的触发条件和执行动作
5. **前端 AI 不直接查询链上**：所有链上查询由后端服务执行；AI 仅输出结构化请求

### 📝 执行示例

**用户输入**："当 Vitalik 买 xx 币时，帮我也买 100 美元"

**AI 处理流程**：
1. 解析为自动策略 Intent
2. 提取参数：目标钱包、代币、金额
3. 输出 StrategyCard 供用户确认
4. 用户确认后，系统注册监听并创建策略

---

## 🎯 Agent 能力矩阵

### A. 信息查询能力
1. ✅ 查询代币基本信息（名称、符号、精度）
2. ✅ 查询市场价格数据（价格、流动性、24h 交易量）
3. ✅ 输出风险评分（基于流动性、交易量、税费等）
4. ✅ 查询最近交易记录
5. ✅ 查询 DeFi 数据（TVL、APY）

### B. 交易执行能力
1. ✅ Swap 交易生成（通过 0x API）
2. ✅ Approve 流程生成
3. ✅ 交易预览（显示 Approve + Swap 步骤）
4. ⏳ 添加/赎回流动性（计划中）
5. ⏳ 跨链桥（计划中）

### C. 自动化能力
1. ✅ 创建自动交易策略
2. ✅ 价格触发条件（计划中）
3. ✅ 钱包行为监听（计划中）
4. ✅ 触发器注册与管理
5. ✅ 任务队列处理
6. ⏳ 定时任务（计划中）

### D. 风险防护能力
1. ✅ 价格数据检测
2. ✅ 流动性检测（最低阈值）
3. ✅ 交易量检测（最低阈值）
4. ✅ 代币税费检测
5. ✅ 流动性锁定检测

### E. 意图解析能力
- ✅ 自然语言 → 结构化 JSON Intent
- ✅ 支持多种 Intent 类型：`token_info`、`swap`、`auto_buy`、`auto_sell`、`monitor_wallet` 等

---

## 💬 1. Chat 主界面

### 核心功能

#### AI 对话交互
用户通过自然语言与 AI 交互，AI 能够：
- 理解用户意图并转换为结构化指令
- 结合所有链上的数据提供实时查询
- 提供跨链、跨协议的数据分析
- 生成交易预览和执行计划

#### 结构化卡片输出
AI 根据用户需求输出不同类型的卡片：
- **TokenCard**：代币信息卡片（价格、流动性、风险评分等）
- **StrategyCard**：自动交易策略卡片
- **ExecutionPreviewCard**：交易执行预览卡片
- **MarketDataCard**：市场数据卡片
- **AIReportCard**：跨页分析卡片，可在聊天内生成并在页面内展示

#### 使用场景示例
- "查询 ETH 在 Uniswap 和 Sushiswap 上的流动性情况"
- "Base 链上某个代币的市场动态"
- "查询 0x123... 这个代币的信息"
- "当这个币跌 30% 时，帮我买 100 USDC"
- "用 100 USDC 买 DEGEN"

### 交互流程
1. 用户在聊天框输入自然语言
2. AI 解析意图并调用相应 API
3. 返回结构化卡片供用户查看
4. 用户确认后执行操作（如交易）

### 前端交互

#### 主界面
- ChatGPT/Gemini 风格的聊天布局：用户消息在右侧气泡、AI 消息在左侧无气泡内容块
- 返回结构化卡片（TokenCard、StrategyCard、ExecutionPreviewCard、AIReportCard）
- 点击卡片的链接可触发 `agent-link` 导航

#### 卡片类型

##### TokenCard
当用户发送合约地址时自动生成，包含：
- 代币基本信息
- 市场数据（价格、流动性、交易量）
- 风险评分
- 最近交易记录

##### StrategyCard
创建自动交易策略时生成，包含：
- 策略类型（买入/卖出）
- 目标代币
- 触发条件
- 执行金额
- 安全约束

##### ExecutionPreviewCard
交易执行前预览，包含：
- 交易步骤（Approve + Swap）
- 预估输出
- 滑点设置
- 风险提示
- 跨页链接：到 MarketData/News/SuperDefi 的相关模块，查看更详细数据或继续操作

##### 手动交易确认卡片
**内容要素**：
- 实时价格、24h 交易量、流动性、市场深度（买卖盘）

**交互设计**：
- 简洁引导：分步提示（选择链/代币、输入金额、设置滑点）
- 提供模板化填写与步骤说明，逐步完成确认
- 帮助用户在交易前做出知情决策，与聊天生成的 ExecutionPreviewCard 协同展示

---

## 📰 2. News 新闻情报页面

### 功能模块

#### News（新闻）
**数据源**：
- CoinDesk、The Block、Decrypt、CryptoSlate、CoinTelegraph 等主流媒体
- 协议官方公告
- 监管政策更新

**内容分类**：
- DeFi：DeFi 协议相关新闻
- Technology：技术更新和升级
- Regulation：监管政策变化
- NFT：NFT 市场动态
- Market：市场整体动态

**功能特性**：
- 重要事件提醒：网络升级、治理投票、代币发布等
- 政策影响分析：监管政策变化对市场的影响
- 项目公告：新协议发布、功能更新
- 时间线展示：按时间顺序展示新闻

#### Social（社交媒体）
**数据源**：
- Twitter：主流加密社区讨论
- Reddit：r/cryptocurrency 等社区
- Farcaster：去中心化社交网络

**功能特性**：
- 市场情绪分析：基于社交媒体的情绪分析
- 热门话题追踪：实时热门讨论主题
- KOL 动态监控：关键意见领袖的观点和操作
- 情绪指数：综合社交媒体的市场情绪评分
- 情绪趋势图：市场情绪变化可视化

#### On-chain（链上数据）
**数据内容**：
- 链上事件：大额转账、合约交互、代币转移
- 资金流动：跨链资金流向和分布
- 交易模式：异常交易模式识别
- 链上指标：活跃地址、交易频率、Gas 使用等

**功能特性**：
- 实时监控：实时展示链上活动
- 异常检测：识别异常交易和资金流动
- 可视化展示：资金流动热力图、交易流向图
- 多链对比：对比不同链上的活动数据

---

## 📊 3. MarketData 市场数据面板

### 功能模块

#### Overview（市场概览）
**全球市场数据**：
- 总市值：加密货币市场总市值
- 24h 交易量：全球 24 小时交易量
- 市场占比：各币种市值占比

**资金流向分析**：
- L1 资金流：以太坊等 L1 链的资金流动
- L2 资金流：Arbitrum、Base 等 L2 的资金流动
- 稳定币流动：USDT、USDC、DAI 的供应量变化

**市场情绪指标**：
- 恐惧贪婪指数：综合多个指标的市场情绪
- 情绪趋势：情绪指数的历史变化
- 情绪分布：不同情绪区间的分布

**涨跌排行榜**：
- Top 涨幅：24h 涨幅最大的代币
- Top 跌幅：24h 跌幅最大的代币
- 涨跌分布：涨跌代币的分布统计

**稳定币分析**：
- 供应量变化：USDT、USDC、DAI 的供应量变化
- 稳定币流动：稳定币在各链间的流动

#### Chains（链数据）
**多链统计**：
- 支持链：Ethereum、BNB、Base、Arbitrum、Polygon、Solana、TON 等
- 链上指标：TPS、Gas 价格、活跃钱包数
- TVL 数据：各链总锁定价值
- 跨链资产：桥接资产统计
- 交易统计：24h 交易数量

**链对比分析**：
- 性能对比：TPS、Gas 费用对比
- 生态对比：TVL、活跃用户对比
- 增长趋势：各链的增长趋势对比

#### Tokens（代币分析）
**代币搜索**：
- 合约地址搜索：输入合约地址查询代币信息
- 代币名称搜索：通过名称或符号搜索
- 多链搜索：支持跨链搜索同一代币

**市场数据展示**：
- 价格信息：当前价格、24h 变化、历史价格
- 流动性数据：流动性池大小、流动性分布
- 交易量数据：24h 交易量、交易量趋势
- 市值排名：市值和排名信息

**数据可视化**：
- K 线图表：价格走势 K 线图
- 交易量图表：买卖量对比柱状图
- 流动性图表：流动性变化趋势
- 价格深度图：买卖盘深度

**风险评分**：
- 综合风险评分：基于多维度指标的风险评分
- 风险项明细：流动性、交易量、税费、锁定等
- 风险趋势：风险评分的历史变化

**热门代币展示**：
- 多维度筛选：基于涨幅、交易量、市场情绪、社交提及等维度进行筛选
- 简洁视图：采用简洁卡片视图（高度 100–140px），避免界面复杂化，同时提供足够信息支持决策，必要时可展开查看更多详情

#### Activity（链上活动）
**大户交易监控**：
- 大额交易：实时展示大额交易
- 交易流向：资金流向分析
- 大户持仓：大户持仓变化

**聪明钱包活动**：
- 智能钱包识别：识别并跟踪智能钱包
- 交易行为分析：分析聪明钱包的交易模式
- 跟单机会：识别可跟单的交易机会

**热门代币追踪**：
- 24h 交易量排行：交易量最大的代币
- 价格变化排行：价格变化最大的代币
- 社交提及排行：社交媒体提及最多的代币

**链上统计**：
- 合约部署：各链新合约部署统计
- 流动性变化：添加/移除流动性统计
- 代币转移：大额代币转移统计

#### Risk（风险分析）
**风险雷达图**：
- 多维度风险评分：价格、流动性、交易量、税费等
- 风险可视化：雷达图展示各维度风险
- 风险等级：低、中、高、极高风险等级

**风险检测项**：
- 价格数据可用性：检查价格数据是否可靠
- 流动性充足性：检查流动性是否充足
- 交易量充足性：检查交易量是否充足
- 代币税费检测：检测买卖税费
- 流动性锁定检测：检测流动性是否锁定
- 价格偏差检测：市场价格与交换价格偏差检查（偏差 >50% 禁止交易）

**风险评估报告**：
- 综合评分：综合所有风险项的评分
- 风险建议：基于风险评分的建议
- 历史风险：历史风险评估记录

**风险评估交互优化**：
- **弹窗与教程**：当触发潜在风险（如价格偏差过大）时，弹出带有详细解释的窗口，附带简短教程与相关技术原理
- **详细解释与技术原理**：例如"价格偏差超 50%"，给出计算方式 `|swap_price - market_price| / market_price × 100%`，并说明潜在影响（滑点损失、MEV 风险、流动性稀薄）
- **规则一致性**：严格遵守"价格偏差 > 50% 禁止交易"的平台规则，并在卡片与预览中明确告知
- **动态阈值与交易警告**：
  - 警告框展示：超过预设安全阈值时提示价格偏差百分比与"超出安全范围"，按严重程度分级：轻微（<10%）、中等（10–30%）、严重（30–50%）、阻断（≥50%）
  - 二次确认：在严重警告场景下，需用户二次确认是否继续交易，确保用户充分理解潜在风险

### 数据来源
- **链上数据**：RPC 节点、链上索引器（TheGraph、Goldsky）
- **市场数据**：DexScreener、DeFiLlama、CoinGecko
- **价格数据**：多个价格预言机（Chainlink、Band Protocol）

---

## 🏦 4. SuperDefi DeFi 协议整合

### 支持的协议

#### DEX（去中心化交易所）
- **Uniswap**：最大的去中心化交易所
- **Sushiswap**：社区驱动的 DEX
- **Curve**：稳定币和相似资产交易

#### 借贷协议
- **Aave**：去中心化借贷协议
- **Compound**：算法利率协议
- **MakerDAO**：稳定币和借贷协议

#### 衍生品
- **GMX**：去中心化衍生品交易
- **dYdX**：去中心化衍生品交易所

#### 流动性质押
- **Lido**：以太坊流动性质押
- **Rocket Pool**：去中心化质押池

### 功能模块

#### Protocol Overview（协议概览）
**核心指标**：
- TVL：总锁定价值
- 24h 交易量：协议交易量统计
- 活跃池数量：当前活跃的流动性池/市场
- 用户数量：协议活跃用户数
- 协议代币价格：协议原生代币价格

**数据可视化**：
- TVL 趋势图：TVL 历史变化
- 交易量趋势图：交易量历史变化
- 用户增长图：用户数量增长趋势

#### Protocol Details（协议详情）
**协议介绍**：
- 协议功能：协议的主要功能和使用场景
- 协议特点：协议的核心特点和优势
- 使用指南：如何使用该协议

**核心指标展示**：
- 实时数据：当前协议的各项指标
- 历史数据：历史数据对比
- 多链数据：协议在各链上的数据分布

**关键池/市场**：
- 主要流动性池：协议的主要流动性池
- 交易对信息：主要交易对的数据
- 池深度：各池的流动性深度

**风险评分**：
- 协议风险评估：基于多维度指标的协议风险评分
- 风险项明细：各项风险的详细说明
- 风险趋势：风险评分的历史变化

**用户活动分析**：
- 用户交互数据：用户与协议的交互统计
- 交易模式：用户的交易模式分析
- 用户分布：用户的地域和类型分布

**代币信息**：
- 协议代币详情：协议原生代币的信息
- 代币分配：代币分配和释放计划
- 代币经济：代币经济学模型

#### Protocol List（协议列表）
**协议选择器**：
- 协议列表：所有支持的协议列表
- 快速切换：快速切换不同协议
- 协议搜索：通过名称搜索协议

**协议分类**：
- 按类型筛选：DEX、借贷、衍生品、质押等
- 按链筛选：按支持的链筛选
- 按 TVL 排序：按 TVL 大小排序

**协议对比**：
- 多协议数据对比：对比不同协议的数据
- 功能对比：对比不同协议的功能
- 优势分析：各协议的优势分析

### 数据查询功能

#### 流动性池查询
- 池信息：池的地址、代币对、流动性大小
- 池深度：买卖盘深度
- 池历史：池的历史数据
- APY 计算：池的收益率计算

#### 借贷利率查询
- 实时利率：各资产的借贷利率
- 利率历史：利率的历史变化
- 利用率：资金利用率
- 利率预测：基于历史数据的利率预测

#### 交易对查询
- 交易对列表：所有可用的交易对
- 交易对数据：价格、流动性、交易量
- 交易对深度：买卖盘深度
- 交易对历史：历史交易数据

#### APY 查询
- 实时 APY：各池/市场的实时收益率
- APY 历史：收益率的历史变化
- APY 对比：不同池/市场的收益率对比
- APY 计算：收益率计算方式说明

### 数据来源
- **链上数据**：直接查询各协议合约
- **DeFiLlama**：协议 TVL 和 APY 数据
- **协议 API**：各协议官方数据接口
- **链上索引器**：TheGraph、Goldsky 等索引器

---

## 🔔 5. 通知与风险预警系统

### 通知功能

#### 自定义通知阈值
用户可设定以下通知阈值：
- **价格阈值**：代币价格达到设定值时通知
- **流动性阈值**：流动性变化达到设定值时通知
- **交易量阈值**：交易量达到设定值时通知
- **社交提及阈值**：社交提及达到设定值时通知

#### 市场波动提醒
- **大规模波动**：市场发生大规模波动时自动提醒
- **异常交易**：检测到异常交易时提醒
- **资金流动**：大额资金流动时提醒

#### 风险预警
- **风险等级变化**：代币风险等级变化时提醒
- **流动性风险**：流动性不足时提醒
- **价格异常**：价格异常波动时提醒
- **合约风险**：检测到合约风险时提醒

#### 重要新闻推送
- **重要事件**：重要市场事件实时推送
- **政策更新**：监管政策更新时推送
- **协议升级**：协议重要升级时推送

### 通知渠道
- **应用内通知**：应用内实时通知
- **Web 推送**：浏览器推送通知
- **邮件通知**：重要事件邮件通知（可选）

---

## 🎯 6. 个性化推荐系统

### 行为分析
- **查询历史**：分析用户的查询历史
- **交互行为**：分析用户的交互行为
- **关注偏好**：分析用户关注的链、协议、资产

### 个性化推荐
- **市场动态推荐**：基于兴趣推送相关市场分析
- **代币推荐**：推荐用户可能感兴趣的代币
- **协议推荐**：推荐用户可能感兴趣的协议
- **新闻推荐**：推荐用户可能感兴趣的新闻

### 关注列表
- **关注链**：关注特定链的动态
- **关注协议**：关注特定协议的动态
- **关注资产**：关注特定资产的动态
- **关注钱包**：关注特定钱包的活动

### 动态数据展示
- **个性化仪表板**：根据用户需求定制仪表板
- **数据优先级**：根据用户兴趣调整数据展示优先级
- **自定义视图**：用户可自定义数据展示视图

---

## 🤖 7. AI 分析与跨页联动（AgentLink）

### 目标
让 Agent 打通网站内各页面的壁垒，实时生成分析报告，并在 Chat 与页面之间相互联动。

### AI 分析报告（AIReportCard）
- 在 MarketData/News/SuperDefi 等页面顶部展示
- 包含来源、洞察、指标、建议与相关链接
- 点击可跳转或在聊天生成卡片
- 卡片内容与聊天卡片契约一致，保持体验统一

### 事件与协议
- **前端订阅**：前端订阅 `agent-link` 事件
- **导航协议**：`agent://navigate?to=/marketdata/tokens&id=...`
- **卡片协议**：`agent://open-card?type=AIReportCard&payload=...`
- **统一事件总线**：`agent-link`

### 后端接口
- `POST /agent/report`：生成页面分析报告卡片（AIReportCard）
- `POST /agent/link`：创建跨页联动动作（导航、打开卡片、订阅触发器）

### 上下文传递
在 Intent 中携带 `context.page`、`context.section`、`link.targets[]`，用于精准落位与跳转。

### 页面要求
所有主页面需预留 AIReport 区域，支持实时展示 AI 生成的分析报告。

---

## 📊 8. 盈亏展示与历史记录

### 数字总览
实时展示策略盈亏、累计 PnL、胜率、最大回撤等核心指标。

### 历史交易明细
- 记录交易日期、执行价格、滑点、交易量与费用
- 支持按时间/资产/策略筛选
- 保留每笔交易的决策过程与审计摘要
- 支持筛选与导出（CSV）

### 日志与审计
- 审计事件字段：`intent_id`、`user_id`、`risk_id`、`tx_hash`、`quote_snapshot`、`decision`
- 所有操作记录在审计日志中，支持查询和导出

---

## 🔄 工作流程示例

### 场景 1：查询代币信息
```
用户："0x123..."
  ↓
AI 解析为 token_info intent
  ↓
调用 GET /agent/tokens/:address
  ↓
返回 TokenCard
  ↓
前端显示卡片
```

### 场景 2：创建自动交易策略
```
用户："当这个币跌 30% 时，帮我买 100 USDC"
  ↓
AI 解析为 auto_buy intent，提取参数
  ↓
调用 POST /agent/strategies
  ↓
返回 StrategyCard
  ↓
用户确认
  ↓
注册触发器到 POST /agent/triggers
  ↓
策略生效，等待触发条件
```

### 场景 3：执行 Swap 交易
```
用户："用 100 USDC 买 DEGEN"
  ↓
AI 解析为 swap intent
  ↓
调用 POST /agent/tx/preview 生成交易计划
  ↓
执行风险检查
  ↓
返回 ExecutionPreviewCard
  ↓
用户确认
  ↓
执行交易（需要用户签名）
```

---

## 📊 系统状态监控

Agent 提供状态查询接口：
- `/agent/status` - 查看策略数量、触发器数量、队列大小
- `/agent/health` - 系统健康检查
- `/agent/audit` - 查看操作审计日志
- `/agent/queue` - 查看任务队列状态

---

## 🔐 安全机制

### 核心原则
1. **无私钥托管**：系统不存储用户私钥
2. **基于授权额度**：自动交易使用 ERC20 Allowance，用户可随时撤销
3. **交易限制**：合约限制最大交易额度，仅允许特定 DEX 路径
4. **用户确认**：所有交易操作需要用户明确确认
5. **可审计性**：所有操作记录在审计日志中
6. **密钥合规**：AI 不生成或传输助记词/私钥；导出仅在用户本地钱包端完成；系统从不留存密钥

### 风险检查
每次交易前自动执行：
- 市场价格与交换价格的检查
- 流动性充足性检查
- 交易量充足性检查
- 代币税费检查
- 流动性锁定检查

---

## 📱 页面路由结构

```
/                    → Chat 主界面
/news                → News 新闻情报页面
/marketdata          → MarketData 面板
  /overview          → 市场概览
  /chains            → 链数据
  /tokens            → 代币分析
  /activity          → 链上活动
  /risk              → 风险分析
/superdefi           → SuperDefi 协议面板
  /protocol/:id      → 协议详情页
```

---

## 📝 技术栈

- **前端**：React + TypeScript + Vite
- **后端**：Node.js + Fastify
- **AI**：DeepSeek API
- **区块链交互**：0x API、RPC 节点
- **数据源**：DexScreener、DeFiLlama、CoinGecko
- **钱包**：Privy（托管钱包）

---

## 🚀 未来扩展

### 计划中的功能
- ⏳ 定时任务（DCA、定期买入）
- ⏳ 跨链桥集成
- ⏳ NFT 交易
- ⏳ 流动性管理（添加/移除 LP）
- ⏳ 合约分析
- ⏳ 钱包健康评分
- ⏳ 税务模块

### 模块化设计
系统采用模块化架构，新功能可以作为插件扩展，不影响核心功能。

---

## ⚠️ 重要说明

1. **不提供投资建议**：本平台所有数据和分析仅用于信息展示，不构成投资建议
2. **用户自行承担风险**：所有交易决策和风险由用户自行承担
3. **数据仅供参考**：市场数据可能存在延迟或不准确，请以官方数据为准
4. **持续更新**：功能和数据源会持续更新，请关注最新版本
5. **密钥合规**：AI 不生成或传输私钥或助记词；导出仅在用户本地钱包端完成
6. **无私钥托管**：系统不存储密钥；所有自动执行基于授权额度且需用户确认
7. **交易需用户确认**：所有交易操作都需要用户明确确认
8. **风险检查不保证安全**：风险检查仅作为参考，不保证绝对安全

---

## 📚 相关文档

- [API 文档](./backend/docs/api.md)
- [开发指南](./backend/docs/development-guide.md)
- [交互设计](./backend/docs/interaction.md)
