# KiKo 文档

文档样式参考 [Base Docs](https://docs.base.org)。

## 本地启动

```bash
# 安装 Mint CLI（与 Base 文档相同）
npm i -g mint

# 进入 docs 目录
cd docs

# 启动文档服务
mint dev
```

访问 http://localhost:3000

> 若使用 `mintlify dev` 报错，请改用 `mint dev`（本项目已迁移至 docs.json 格式）
