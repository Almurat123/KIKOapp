# Scripts

Placeholder for migrated CLI scripts from kiko-api/scripts.

- Security scripts (runtime-detectors, fund-flows, etc.) can be migrated here as separate `main` packages.
- Run migrations: use `kikoapi` with `MIGRATE_UP=1` or a dedicated `migrate` command.
